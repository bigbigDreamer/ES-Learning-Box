/**
 *@Time  2018/8/7 14:16
 *@Author  沐沐
 *@Description Array的扩展
 *      1.Array.prototype.indexOf(value):得到值在数组中的第一个下标
 *      2.Array.prototype.LastIndexOf(value):得到值在数组中的最后一个下标
 *      3.Array.prototype.forEach(function(item,index){});遍历数组
 *      4.Array.prototype.map(function(item,index){});遍历数组，返回一个新的数组，返回加工之后的值
 *      (通常返回一个加减运算值，例如 return item+5)
 *      5.Array.prototype.filter(function(item,index){})；遍历过滤出的一个新数组，返回条件为true的值
 *      （通常返回的是一个boolean表达式  ；例如：return item>3）
 *@return
 **/